# V40 修复交付说明

## 对 V39 缺陷目录的处理

已修复或缓解：

- D39-01：路线模板真实分流，未实现路线不再伪装成 Ridge。
- D39-02：终审主指标改为题型候选指标优先，并显式声明方向。
- D39-03：数据选择改为质量评分，不再最小文件优先。
- D39-04：捕获 `TimeoutExpired`，失败写入 registry。
- D39-05：V40 默认输出目录改为 `run/v40_auto_solver`。
- D39-06：生成代码只看 `ROUTE['role']`，不按 route 名称猜 baseline。
- D39-07：预测类采用时间顺序切分。
- D39-08：增加 `minimum_improvement=0.01`，低增益打回。
- D39-09：记录原始/最终代码 SHA-256 和 repair_log。
- D39-10：CSV/TXT 读取支持 `utf-8/gb18030/gbk/utf-16` 回退。
- D39-11：目标列未知时拒绝任意最后一列回归。
- D39-13：测试题面改为正常中文，并新增多项负向测试。
- D38-01：修复 V38 报告中文文案。
- D38-02：2012A 的 `equal_judge_average` 改为中位数聚合，避免和 first-panel baseline 完全重复。
- D38-03：补充部分 lower-better 指标。

部分保留为后续增强：

- D39-12：当前仍是关键词检索，后续应接入向量索引和领域方法库。

## 验证结果

```text
12 passed
V38 gate.verdict=pass
V40 bundled_reference_demo verdict=revise
```

V40 demo 返回 `revise` 是正确行为：它不再把“代码能跑”误判为“模型可信”。
